version: '3.8'

services:
  churn_api:
    build: ..
    container_name: churn_cltv_api
    ports:
      - "8000:8000"
    volumes:
      - ../models:/app/models
    environment:
      - MODEL_DIR=/app/models
